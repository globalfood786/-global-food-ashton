GLOBAL FOOD ASHTON - EXACT MASTER DESIGN VERSION

The supplied homepage image is now displayed directly as the first visible homepage, so opening index.html will show the same design supplied in the conversation.

Below the master design is a working order section with basket and WhatsApp checkout.

Important: the master design itself is a single image. Individual controls inside that image are visual only. The ordering section below it is functional. A later development pass can recreate each part of the master design as individual HTML components while preserving the same appearance.
